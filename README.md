# maven-nexus-deploy

*It includes settings.xml with deloyment user.

To create deployment user first create nx-deploy role with previllages nx-repository-view-*-*-* then create user called deployment with password = password and assign nx-role. make changes to settings.xml to reflect these changes.

